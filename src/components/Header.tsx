import Navbar from './Navbar';
import { HeaderSection, Title } from '../StyledComponents';

function Header() {
  return (
    <HeaderSection>
      <Title>SparkGoods</Title>
      <Navbar />
    </HeaderSection>
  );
}

export default Header;
